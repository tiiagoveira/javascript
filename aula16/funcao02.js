// no caso de n1 e n2 estarem igual a zero significa que
// se um dos dois não for passado como parâmetro ele vai considerar o valor como zero
function soma(n1=0, n2=0){
    return n1 + n2
}

console.log(soma(5))